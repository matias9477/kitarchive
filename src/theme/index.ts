/**
 * Theme package — centralized design tokens (dark-only, from DESIGN.md).
 */
export { palette, colors } from "./colors";
export { typography, fonts } from "./typography";
export { spacing, radius } from "./layout";
export { getThemeColors, useTheme } from "./utils";
export type { Theme, ThemeColors } from "./utils";
